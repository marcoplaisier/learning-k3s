apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: inventory
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
